git log --oneline(show list of commits or action
git branch login(create new login branch))
git checkout login(enter login branch)
git status(check status for unpushed commits nd shits)
git branch -a(all branch lost)
git checkout -b login(create nd switch to logi)